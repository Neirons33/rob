IND_indian_unification = {

 IND_invade_bengal = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  BNL = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = BNL

  has_war = no 
  NOT = {
   is_in_faction_with = BNL
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = BNL
}
  }
 }

 IND_invade_odisha = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  ORS = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = ORS

  has_war = no 
  NOT = {
   is_in_faction_with = ORS
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = ORS
}
  }
 }

 IND_invade_prf = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  PRF = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = PRF

  has_war = no 
  NOT = {
   is_in_faction_with = PRF
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = PRF
}
  }
 }

 IND_invade_rpf = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  RPF = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = RPF

  has_war = no 
  NOT = {
   is_in_faction_with = RPF
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = RPF
}
  }
 }

 IND_invade_mysore = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  MYS = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = MYS

  has_war = no 
  NOT = {
   is_in_faction_with = MYS
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = MYS
}
  }
 }

 IND_invade_tamfed = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  TML = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = TML

  has_war = no 
  NOT = {
   is_in_faction_with = TML
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = TML
}
  }
 }

 IND_invade_madras = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  BNL = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = MRS

  has_war = no 
  NOT = {
   is_in_faction_with = MRS
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = MRS
}
  }
 }

 IND_invade_kashmir = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  JAK = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = JAK

  has_war = no 
  NOT = {
   is_in_faction_with = JAK
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = JAK
}
  }
 }

 IND_invade_gujarat = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  GUJ = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = GUJ

  has_war = no 
  NOT = {
   is_in_faction_with = GUJ
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = GUJ
}
  }
 }

 IND_invade_hyderabad = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  HYD = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = HYD

  has_war = no 
  NOT = {
   is_in_faction_with = HYD
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = HYD
}
  }
 }


 IND_invade_bombay = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  BBY = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = BBY

  has_war = no 
  NOT = {
   is_in_faction_with = BBY
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = BBY
}
  }
 }

 IND_invade_cir = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  CIR = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = CIR

  has_war = no 
  NOT = {
   is_in_faction_with = CIR
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = CIR
}
  }
 }

 IND_invade_sind = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  SNH = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = SNH

  has_war = no 
  NOT = {
   is_in_faction_with = SNH
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = SNH
}
  }
 }

 IND_invade_baluchistan = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  BAL = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = BAL

  has_war = no 
  NOT = {
   is_in_faction_with = BAL
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = BAL
}
  }
 }

 IND_invade_bihar = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  BIH = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = BIH

  has_war = no 
  NOT = {
   is_in_faction_with = BIH
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = BIH
}
  }
 }

 IND_invade_punjab = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  PNJ = { exists = yes }
  has_army_manpower = {
   size > 30000
  }
is_neighbor_of = PNJ

  has_war = no 
  NOT = {
   is_in_faction_with = PNJ
   is_puppet = yes
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = PNJ
}
  }
 }

 IND_invade_delhi = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  DEL = { exists = yes }
  has_army_manpower = {
   size > 150000
  }

is_neighbor_of = DEL
date < 1939.01.01

  has_war = no 
  NOT = {
   is_in_faction_with = DEL
   is_puppet = yes
	tag = DEL
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = DEL
}
  }
 }

 IND_invade_assam = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  ASM = { exists = yes }
  has_army_manpower = {
   size > 150000
  }

is_neighbor_of = ASM
date < 1939.01.01

  has_war = no 
  NOT = {
   is_in_faction_with = ASM
   is_puppet = yes
	tag = ASM
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = ASM
}
  }
 }

 IND_invade_braj = {
  ai_will_do = {
   base = 200
  }
  fire_only_once = yes
  available = {
  BRJ = { exists = yes }
  has_army_manpower = {
   size > 150000
  }

is_neighbor_of = BRJ
date < 1939.01.01

  has_war = no 
  NOT = {
   is_in_faction_with = BRJ
   is_puppet = yes
	tag = BRJ
  }
  
  }
  complete_effect = {
		create_wargoal = {
			type = annex_everything		
			target = BRJ
}
  }
 }


}